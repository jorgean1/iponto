{
  "name": "iponto",
  "version": "1.1.0",
  "private": true,
  "description": "Agendador autônomo de ponto para o Controle de Projetos",
  "type": "module",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test",
    "install-browser": "playwright install chromium"
  },
  "engines": { "node": ">=20" },
  "dependencies": {
    "express": "^5.1.0",
    "luxon": "^3.7.2",
    "nodemailer": "^9.1.0",
    "playwright": "^1.55.0"
  }
}
