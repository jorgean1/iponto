# Checkpoint de contexto

## Data

2026-09-01 18:15 (America/Sao_Paulo)

## Objetivo atual

Construir e entregar o Iponto: aplicação autônoma para agendar entrada, almoço, retorno e saída no Controle de Projetos, enviar alertas por e-mail, oferecer interface Windows/Android, instalador, manual e versionamento Git.

## Projeto ativo

- Pasta: `D:\iponto`
- Aplicação: Node.js 22, Express, Playwright, Nodemailer, Luxon e PWA em HTML/CSS/JavaScript.
- URL local: `http://localhost:3077`
- URL correta do sistema externo: `https://controledeprojetos.crptecnologia.com.br/Lancamentos`

## O que já foi feito

- Tela responsiva com horários, credenciais, SMTP, ativação, testes manuais e histórico.
- Credenciais criptografadas em repouso com AES-256-GCM; chave local em `data/iponto.key`.
- Agendador com fuso `America/Sao_Paulo` e tolerância de retomada de 15 minutos.
- Entrada e retorno escolhem aleatoriamente um controle visível com texto `Iniciar`.
- Almoço e saída priorizam `a.btnParar[href*="/Lancamentos/PararAtividade"]`.
- Teste de acesso valida HTTP, login e controles sem registrar ponto.
- Alertas SMTP de sucesso/falha e teste de e-mail.
- Botão Limpar histórico com confirmação; configurações são preservadas.
- PWA acessível no Android pela rede local; automação permanece no Windows.
- Tarefa `Iponto` instalada no Agendador do Windows e iniciada.

## Arquivos relevantes

- `src/server.js`: API e servidor web.
- `src/scheduler.js`: horários, tolerância e execução.
- `src/automation.js`: login e controles Iniciar/Parar.
- `src/store.js`: persistência e criptografia.
- `src/mailer.js`: SMTP.
- `public/`: PWA.
- `tests/`: testes de automação e armazenamento.
- `installer/`: instalador, desinstalador e empacotamento.
- `scripts/generate_manual.py`: geração do manual em PDF.
- `output/pdf/Manual-do-Iponto.pdf`: manual do usuário.

## Comandos e validações

- `npm install` e `npx playwright install chromium` concluídos.
- `npm audit`: zero vulnerabilidades conhecidas após atualização do Nodemailer.
- `npm test`: testes de detecção dos controles, criptografia e limpeza de histórico aprovados.
- A URL `/Lancamento` retornava 404; corrigida para `/Lancamentos`, que redireciona corretamente ao login.
- Teste real informado pelo usuário: HTTP 200, login realizado e controle Iniciar encontrado.
- Diagnóstico de ponto perdido: serviço estava parado; tarefa automática não existia. Tarefa criada e verificada como Running, porta 3077 ativa e `/api/health` retornando scheduler active.

## Estado atual

- Automação habilitada.
- Horários que estavam salvos: 07:56, 12:04, 13:56 e 18:04.
- Serviço deve ser iniciado pelo Agendador do Windows no logon.
- O ponto de 18:04 de 01/09/2026 foi registrado como perdido, sem execução retroativa.
- Não registrar credenciais ou senhas em commits, manuais ou checkpoints.

## Decisões assumidas

- A automação depende de um Windows ligado e com usuário conectado; Android é somente interface.
- Pontos atrasados em até 15 minutos podem ser recuperados; depois disso são marcados como perdidos.
- Nenhum teste de acesso aciona Iniciar/Parar.
- A seleção de atividade é aleatória entre controles Iniciar visíveis, conforme pedido do usuário.

## Pendências

- Confirmar no dia seguinte se a tarefa automática permaneceu ativa e executou o primeiro horário.
- Para publicar em Git remoto, definir provedor, conta/organização e visibilidade. O repositório local será preparado sem `data/`, chaves ou credenciais.
- Validar o instalador em uma máquina Windows limpa quando disponível.

## Próximos passos sugeridos

1. Executar o instalador gerado em ambiente de teste.
2. Configurar o Iponto e executar Testar e-mail e Testar acesso.
3. Confirmar o histórico após o próximo horário agendado.
4. Adicionar o remoto Git solicitado e enviar a branch principal.
